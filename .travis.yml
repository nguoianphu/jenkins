dist: trusty
language: java
sudo: false
jdk:
  - openjdk8
  - oraclejdk8
script:
  - export MAVEN_OPTS="-XX:MaxPermSize=128m"
  - mvn clean package -pl war -am -DskipTests -Dfindbugs.skip
  - ls -la war/target/